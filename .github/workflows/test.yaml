name: Try to test NEw PR Assiner
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Test PR Assigner
        uses: nookyo/.github/actions/pr-assigner@main
